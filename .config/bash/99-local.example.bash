# shellcheck shell=bash

# Copy this file to 99-local.bash for machine-specific or private settings.
# 99-local.bash is ignored by git and sourced automatically by ~/.bashrc.

# Private tokens and credentials belong in 99-local.bash, not committed files.
# export OPENAI_API_KEY="replace-me"

# Local paths and work-only settings are also good candidates for 99-local.bash.
# export AWS_SHARED_CREDENTIALS_FILE="$HOME/.aws/credentials"
# export PATH="$HOME/bin:$PATH"
